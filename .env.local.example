# Trace data source — "mock" plays the scripted demo; "live" reads via /api/trace from Butterbase.
NEXT_PUBLIC_TRACE_MODE=mock

# Butterbase (server-side only — never expose to the browser)
BUTTERBASE_BASE_URL=https://api.butterbase.ai/v1
BUTTERBASE_APP_ID=app_4lls3kxgops9
BUTTERBASE_API_KEY=
