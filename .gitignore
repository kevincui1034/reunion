# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
sidecar/.build/

# Env / secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Note: the local-only PM workspace (.bridge/) is intentionally NOT listed here.
# It is excluded via .git/info/exclude so it never appears in the shared repo.
.rocketride/
